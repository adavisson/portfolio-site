import * as React from 'react';
import '../styles/styles.scss';

const BlogPage: React.FC = () => {
  return (
    <main>
      <title>Blog</title>
      <h1>Blog</h1>
    </main>
  );
};

export default BlogPage;
